{
    "build": {
      "env": {
        "NODE_ENV": "production"
      },
      "commands": [
        "npm install --legacy-peer-deps",
        "npm run build"
      ]
    },
    "scripts": {
        "vercel-build": "npm install && next build"
    }

  }
  